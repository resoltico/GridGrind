package dev.erst.gridgrind.cli.discovery;

/** Typed phase purpose for one ordered task phase. */
public enum TaskPhasePurpose {
  PREPARE,
  AUTHOR,
  IMPORT,
  EXPORT,
  INSPECT,
  ANALYZE,
  VERIFY
}
