package dev.erst.gridgrind.excel.foundation;

/** Stable sheet-name rule identifiers for Excel-facing validation. */
public enum ExcelSheetNameRule {
  BLANK,
  TOO_LONG,
  BOUNDARY_QUOTE,
  INVALID_CHARACTER
}
