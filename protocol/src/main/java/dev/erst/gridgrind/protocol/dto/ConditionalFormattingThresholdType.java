package dev.erst.gridgrind.protocol.dto;

/** Threshold kinds exposed for advanced conditional-formatting reports. */
public enum ConditionalFormattingThresholdType {
  NUMBER,
  MIN,
  MAX,
  PERCENT,
  PERCENTILE,
  UNALLOCATED,
  FORMULA
}
