package dev.erst.gridgrind.protocol.dto;

/** Horizontal cell-alignment values supported by the public GridGrind contract. */
public enum HorizontalAlignment {
  GENERAL,
  LEFT,
  CENTER,
  RIGHT,
  FILL,
  JUSTIFY,
  CENTER_SELECTION,
  DISTRIBUTED
}
